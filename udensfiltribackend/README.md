# UdensFiltri backend source directory

Main project documentation, production Docker instructions, and environment setup are maintained in the repository root README:

- `../README.md`

Use this folder as Django project root when running management commands directly (`manage.py`).
