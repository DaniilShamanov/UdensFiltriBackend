services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ${DB_NAME:-plumbing}
      POSTGRES_USER: ${DB_USER:-plumbing}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-plumbing}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
